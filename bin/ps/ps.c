#include <unistd.h>
#include <stdlib.h>
#include <sys/defs.h>
#include <stdio.h>
#include <string.h>

int main(int argc, char *argv[], char *envp[]) {	

        if(0) {
                puts("signal/pid not specified\n");
        }
        else {
               //TODO: char *signal = argv[1];
               // char *pid = argv[2];
		//int pid_t = atoi(pid);
		ps();

        }
 	
	return 0;
}

